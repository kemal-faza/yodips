# Undip SSO Aggregator

Backend (NestJS) + Web (Vue 3) + Mobile (Kotlin) — aggregates Undip SSO services.

## Structure

- `backend/` — NestJS TypeScript API core (SSO auth, Microsoft OIDC, Kulon/SIAP aggregation)
- `web/` — Vue 3 + Vite SPA
- `mobile/` — Kotlin native (Jetpack Compose)