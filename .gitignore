node_modules/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
docs/
# Build output dari tools/deploy/assemble.sh — jangan di-commit
deploy-artifacts/
AGENTS.md
design-reference/
# Real captured SIAP session data (personal) — never commit. Authored SYNTHETIC
# fixtures used by specs (no personal data) are force-added via `git add -f` so
# the rest of this dir (dashboard/probe captures) stays ignored.
backend/test/fixtures/
.codegraph/
.superpowers/
web/.visual-fix/
CLAUDE.md
.claude
memory/
.vercel
# Firebase config (M0, per-project) — never commit
mobile/app/google-services.json
