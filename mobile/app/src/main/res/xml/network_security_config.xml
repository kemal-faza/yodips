<?xml version="1.0" encoding="utf-8"?>
<!-- Production / release config: HTTPS-only. Cleartext is NOT permitted for any
     host. Debug builds override this (src/debug/res/xml) to allow cleartext only
     to the emulator loopback (10.0.2.2 / localhost), never to production hosts. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>