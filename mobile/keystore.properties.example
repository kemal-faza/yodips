# Template release keystore. Salin ke `keystore.properties` (git-ignored) dan isi.
# JANGAN commit nilai nyata. Jalankan `./gradlew signingReport` untuk verifikasi.
storeFile=keystore/undip-sso.jks
storePassword=REPLACE_ME
keyAlias=undip_sso
keyPassword=REPLACE_ME