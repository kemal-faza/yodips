{
  "name": "sso-tools",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "probe": "ts-node siap-probe.ts",
    "test:build": "tsc -p tsconfig.json --outDir .tmp-test",
    "analyze:observability": "ts-node analyze-observability-log.ts",
    "test": "npm run test:build && node --test .tmp-test/siap-probe.test.js .tmp-test/analyze-observability-log.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
